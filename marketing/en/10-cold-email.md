# Cold email (optional channel)

**Who:** agencies and small SaaS teams whose product prints documents: invoicing, e-commerce order systems, booking/tickets, certificates/e-learning, property management. Find them by searching job posts and case studies for "dompdf", "wkhtmltopdf", "mpdf", "puppeteer pdf".

**Subject:** your PDF pipeline

Hi {{name}},

Saw {{company}} runs {{dompdf / wkhtmltopdf / a Puppeteer worker}} for {{invoices / reports}}. If it is like every other one I have seen, someone on your team owns a box that occasionally eats memory and a layout that breaks on one customer's data.

RenderKit is that pipeline as an API: send HTML, get a Chrome-rendered PDF with real CSS support, page numbers and A4/Letter, in under a second. Free plan of 100 renders a month to try it against your worst invoice; paid from $9.

Docs and a PHP/Node client: https://YOUR-HOST/docs

If PDFs are not a problem for you, ignore this; no follow-up.

{{your name}}
